FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt ./

RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8001

ENV PORT=8001
ENV BACKEND_URL=http://backend:4000
ENV ANOMALY_THRESHOLD=50

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8001"]
